#pragma once

#include <string.h>

typedef unsigned int uint;

typedef struct Http {
	
} Http;

typedef struct HttpServer {
	
} HttpServer;

HttpServer newHttpServer(const char* status);
char* response();
